{
  "type": "commonjs",
  "scripts": {
    "dev": "tsx ./src",
    "bundle": "rimraf dist && tsup",
    "build": "powershell ./build.ps1",
    "start": ".\\bin\\main.exe",
    "debug": "npm run bundle && npm run build && npm run start"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "rimraf": "^6.0.1",
    "tsup": "^8.2.4",
    "typescript": "^5.6.2"
  }
}